# Mirrors [project.dependencies] in pyproject.toml, for the plain
# `pip install -r requirements.txt` path.
nornir>=3.4.1
nornir-netmiko>=1.0.0
netmiko>=4.2.0
Jinja2>=3.1.2
PyYAML>=6.0

# Only needed for waf, --netbox or --open-change / --change (ServiceNow):
#   pip install 'requests>=2.31'
# Only needed for AWS Secrets Manager:
#   pip install 'boto3>=1.34'
# Only needed to run the test suite:
#   pip install 'pytest>=8.0' 'requests>=2.31'
