One choice from 2–4 visible options.

```jsx
<Radio name="scope" value="site" checked={scope==="site"} onChange={...} label="Single site" description="One facility and its circuits." />
```

Stack vertically with 8px gaps; never in a row unless the labels are one word. Above four options, switch to a Select. Always give the group a `name`.
