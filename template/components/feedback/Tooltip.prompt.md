Names an icon-only control or reveals a truncated value.

```jsx
<Tooltip label="Last polled 4 minutes ago"><Icon name="clock" size={14}/></Tooltip>
```

A few words, never a sentence of instruction — if it needs explaining, it needs a FormField hint. Never wrap interactive content: it has no focus trap and disappears on mouse-out.
